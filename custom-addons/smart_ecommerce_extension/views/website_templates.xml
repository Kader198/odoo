<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ============================================================ -->
    <!-- HOMEPAGE - RESPONSIVE CATEGORY FILTERS -->
    <!-- ============================================================ -->
    
    <!-- Homepage Categories Section -->
    <template id="homepage_categories" inherit_id="website.homepage" name="Homepage Categories">
        <xpath expr="//div[@id='wrap']" position="inside">
            <t t-set="categories" t-value="request.env['product.public.category'].sudo().search([('parent_id', '=', False)])"/>
            
            <!-- Hero Section with Categories -->
            <section class="smart-homepage-categories py-5" style="background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);">
                <div class="container">
                    <!-- Section Header -->
                    <div class="text-center mb-4">
                        <h2 class="fw-bold" style="color:rgb(75, 113, 95);">
                            <i class="fa fa-th-large me-2"/>Shop by Category
                        </h2>
                        <p class="text-muted">Browse our wide selection of products</p>
                    </div>
                    
                    <!-- Categories Grid -->
                    <div class="row g-3 justify-content-center">
                        <!-- All Products Card -->
                        <div class="col-6 col-md-4 col-lg-2">
                            <a href="/shop" class="card smart-category-card h-100 text-decoration-none border-0 shadow-sm">
                                <div class="card-body text-center py-4">
                                    <i class="fa fa-shopping-bag fa-3x mb-3" style="color: #714B67;"/>
                                    <h6 class="card-title mb-0">All Products</h6>
                                </div>
                            </a>
                        </div>
                        <!-- Category Cards -->
                        <t t-foreach="categories[:5]" t-as="cat">
                            <div class="col-6 col-md-4 col-lg-2">
                                <a t-att-href="'/shop/category/%s' % slug(cat)" class="card smart-category-card h-100 text-decoration-none border-0 shadow-sm">
                                    <div class="card-body text-center py-4">
                                        <t t-if="cat.image_128">
                                            <img t-att-src="image_data_uri(cat.image_128)" 
                                                 class="rounded-circle mb-3" 
                                                 style="width: 64px; height: 64px; object-fit: cover;"/>
                                        </t>
                                        <t t-else="">
                                            <i class="fa fa-folder-open fa-3x mb-3" style="color: #714B67;"/>
                                        </t>
                                        <h6 class="card-title mb-1"><t t-esc="cat.name"/></h6>
                                        <span class="badge bg-secondary"><t t-esc="len(cat.product_tmpl_ids)"/> items</span>
                                    </div>
                                </a>
                            </div>
                        </t>
                    </div>
                    
                    <!-- View All Categories Link -->
                    <div t-if="len(categories) > 5" class="text-center mt-4">
                        <a href="/shop" class="btn btn-outline-primary">
                            View All Categories <i class="fa fa-arrow-right ms-2"/>
                        </a>
                    </div>
                </div>
            </section>
            
            <!-- Quick Links -->
            <section class="smart-quick-links py-4">
                <div class="container">
                    <div class="row g-3">
                        <div class="col-md-4">
                            <a href="/shop?availability=in_stock" class="card bg-success text-white text-decoration-none h-100">
                                <div class="card-body d-flex align-items-center">
                                    <i class="fa fa-check-circle fa-2x me-3"/>
                                    <div>
                                        <h6 class="mb-0">In Stock</h6>
                                        <small>Available now</small>
                                    </div>
                                </div>
                            </a>
                        </div>
                        <div class="col-md-4">
                            <a href="/shop?order=create_date+desc" class="card bg-info text-white text-decoration-none h-100">
                                <div class="card-body d-flex align-items-center">
                                    <i class="fa fa-star fa-2x me-3"/>
                                    <div>
                                        <h6 class="mb-0">New Arrivals</h6>
                                        <small>Latest products</small>
                                    </div>
                                </div>
                            </a>
                        </div>
                        <div class="col-md-4">
                            <a href="/shop" class="card text-decoration-none h-100" style="background: linear-gradient(135deg, #714B67 0%, #9B6A8F 100%); color: white;">
                                <div class="card-body d-flex align-items-center">
                                    <i class="fa fa-percent fa-2x me-3"/>
                                    <div>
                                        <h6 class="mb-0">Special Offers</h6>
                                        <small>Best deals</small>
                                    </div>
                                </div>
                            </a>
                        </div>
                    </div>
                </div>
            </section>
            
            <!-- ============================================================ -->
            <!-- BECOME A SELLER CTA SECTION -->
            <!-- ============================================================ -->
            <section class="smart-become-seller-cta py-5 my-4" style="background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);">
                <div class="container">
                    <div class="row align-items-center">
                        <!-- Left: Content -->
                        <div class="col-lg-7 text-white mb-4 mb-lg-0">
                            <div class="d-flex align-items-center mb-3">
                                <span class="badge bg-warning text-dark me-2 px-3 py-2">
                                    <i class="fa fa-rocket me-1"/> New
                                </span>
                                <span class="text-warning fw-bold">Join Our Marketplace</span>
                            </div>
                            <h2 class="display-5 fw-bold mb-3">
                                Start Selling Online Today
                            </h2>
                            <p class="lead mb-4 opacity-75">
                                Turn your passion into profit. Join thousands of successful sellers and reach customers across the country. Easy setup, powerful tools, and dedicated support to help you grow.
                            </p>
                            
                            <!-- Benefits -->
                            <div class="row g-3 mb-4">
                                <div class="col-sm-6">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle bg-success d-flex align-items-center justify-content-center me-3" 
                                             style="width: 40px; height: 40px; min-width: 40px;">
                                            <i class="fa fa-check text-white"/>
                                        </div>
                                        <div>
                                            <strong class="d-block">Free to Start</strong>
                                            <small class="opacity-75">No monthly fees</small>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-sm-6">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle bg-info d-flex align-items-center justify-content-center me-3" 
                                             style="width: 40px; height: 40px; min-width: 40px;">
                                            <i class="fa fa-truck text-white"/>
                                        </div>
                                        <div>
                                            <strong class="d-block">Easy Shipping</strong>
                                            <small class="opacity-75">We handle delivery</small>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-sm-6">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle bg-warning d-flex align-items-center justify-content-center me-3" 
                                             style="width: 40px; height: 40px; min-width: 40px;">
                                            <i class="fa fa-users text-dark"/>
                                        </div>
                                        <div>
                                            <strong class="d-block">Large Audience</strong>
                                            <small class="opacity-75">Thousands of buyers</small>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-sm-6">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle bg-danger d-flex align-items-center justify-content-center me-3" 
                                             style="width: 40px; height: 40px; min-width: 40px;">
                                            <i class="fa fa-credit-card text-white"/>
                                        </div>
                                        <div>
                                            <strong class="d-block">Secure Payments</strong>
                                            <small class="opacity-75">Fast payouts</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- CTA Buttons -->
                            <div class="d-flex flex-wrap gap-3">
                                <a href="/become-a-seller" class="btn btn-warning btn-lg px-4 py-3 fw-bold">
                                    <i class="fa fa-store me-2"/>Become a Seller
                                </a>
                                <a href="/stores" class="btn btn-outline-light btn-lg px-4 py-3">
                                    <i class="fa fa-eye me-2"/>View Stores
                                </a>
                            </div>
                        </div>
                        
                        <!-- Right: Visual/Stats -->
                        <div class="col-lg-5">
                            <div class="card border-0 shadow-lg" style="border-radius: 20px; overflow: hidden;">
                                <div class="card-body p-4 text-center" style="background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);">
                                    <div class="mb-4">
                                        <div class="rounded-circle mx-auto d-flex align-items-center justify-content-center" 
                                             style="width: 100px; height: 100px; background: linear-gradient(135deg, #714B67 0%, #9B6A8F 100%);">
                                            <i class="fa fa-store fa-3x text-white"/>
                                        </div>
                                    </div>
                                    <h4 class="fw-bold mb-3" style="color: #1a1a2e;">Why Sellers Love Us</h4>
                                    
                                    <!-- Stats -->
                                    <div class="row g-3 text-start mb-4">
                                        <div class="col-6">
                                            <div class="p-3 rounded" style="background: rgba(113, 75, 103, 0.1);">
                                                <div class="h4 fw-bold mb-0" style="color: #714B67;">10%</div>
                                                <small class="text-muted">Low Commission</small>
                                            </div>
                                        </div>
                                        <div class="col-6">
                                            <div class="p-3 rounded" style="background: rgba(40, 167, 69, 0.1);">
                                                <div class="h4 fw-bold mb-0 text-success">24h</div>
                                                <small class="text-muted">Fast Approval</small>
                                            </div>
                                        </div>
                                        <div class="col-6">
                                            <div class="p-3 rounded" style="background: rgba(23, 162, 184, 0.1);">
                                                <div class="h4 fw-bold mb-0 text-info">∞</div>
                                                <small class="text-muted">Unlimited Products</small>
                                            </div>
                                        </div>
                                        <div class="col-6">
                                            <div class="p-3 rounded" style="background: rgba(255, 193, 7, 0.1);">
                                                <div class="h4 fw-bold mb-0 text-warning">24/7</div>
                                                <small class="text-muted">Support</small>
                                            </div>
                                        </div>
                                    </div>
                                    
                                    <a href="/seller/register" class="btn btn-primary w-100 py-3" style="background: #714B67; border-color: #714B67;">
                                        <i class="fa fa-arrow-right me-2"/>Start Registration Now
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
            
            <!-- Featured Products Section -->
            <section class="smart-featured-products py-5" style="background-color: #ffffff;">
                <div class="container">
                    <div class="text-center mb-5">
                        <h2 class="fw-bold" style="color:rgb(75, 113, 95);">
                            <i class="fa fa-star me-2"/>Featured Products
                        </h2>
                        <p class="text-muted">Discover our best-selling and newest products</p>
                    </div>
                    
                    <t t-set="products" t-value="request.env['product.template'].sudo().search([
                        ('is_published', '=', True),
                        ('sale_ok', '=', True),
                        ('website_published', '=', True)
                    ], limit=12, order='create_date desc')"/>
                    
                    <t t-if="products">
                        <div class="row g-4">
                            <t t-foreach="products" t-as="product">
                                <div class="col-6 col-md-4 col-lg-3">
                                    <div class="card h-100 border-0 shadow-sm product-card" style="transition: transform 0.2s; cursor: pointer;" onmouseover="this.style.transform='translateY(-5px)'" onmouseout="this.style.transform='translateY(0)'">
                                        <a t-att-href="product.website_url" class="text-decoration-none">
                                            <div class="position-relative overflow-hidden" style="height: 200px; background-color: #f8f9fa;">
                                                <t t-if="product.image_1920">
                                                    <img t-att-src="'/web/image/product.template/%s/image_1920' % product.id" 
                                                         t-att-alt="product.name"
                                                         class="w-100 h-100" 
                                                         style="object-fit: cover;"/>
                                                </t>
                                                <t t-else="">
                                                    <div class="d-flex align-items-center justify-content-center h-100">
                                                        <i class="fa fa-image fa-3x text-muted"/>
                                                    </div>
                                                </t>
                                                <t t-if="product.availability_status == 'in_stock'">
                                                    <span class="badge bg-success position-absolute top-0 end-0 m-2">
                                                        <i class="fa fa-check-circle me-1"/>In Stock
                                                    </span>
                                                </t>
                                                <t t-elif="product.availability_status == 'low_stock'">
                                                    <span class="badge bg-warning position-absolute top-0 end-0 m-2">
                                                        <i class="fa fa-exclamation-triangle me-1"/>Low Stock
                                                    </span>
                                                </t>
                                                <t t-elif="product.availability_status == 'out_of_stock'">
                                                    <span class="badge bg-danger position-absolute top-0 end-0 m-2">
                                                        <i class="fa fa-times-circle me-1"/>Out of Stock
                                                    </span>
                                                </t>
                                            </div>
                                            <div class="card-body">
                                                <h6 class="card-title text-dark mb-2" style="min-height: 40px; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">
                                                    <t t-esc="product.name"/>
                                                </h6>
                                                <div class="d-flex justify-content-between align-items-center mb-2">
                                                    <span class="h6 mb-0 text-primary fw-bold">
                                                        <t t-esc="product.list_price" t-options="{'widget': 'monetary', 'display_currency': website.currency_id}"/>
                                                    </span>
                                                    <t t-if="product.brand">
                                                        <span class="badge bg-secondary">
                                                            <t t-esc="product.brand"/>
                                                        </span>
                                                    </t>
                                                </div>
                                            </div>
                                        </a>
                                        <div class="card-footer bg-white border-0 pt-0">
                                            <form action="/shop/cart/update" method="post" class="d-grid">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                <input type="hidden" name="product_id" t-att-value="product.id"/>
                                                <input type="hidden" name="add_qty" value="1"/>
                                                <button type="submit" 
                                                        class="btn btn-primary w-100 btn-sm">
                                                    <i class="fa fa-shopping-cart me-2"/>Add to Cart
                                                </button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>
                        
                        <div class="text-center mt-5">
                            <a href="/shop" class="btn btn-outline-primary btn-lg me-2">
                                View All Products <i class="fa fa-arrow-right ms-2"/>
                            </a>
                            <a href="/stores" class="btn btn-outline-secondary btn-lg">
                                <i class="fa fa-store me-2"/>Browse Stores
                            </a>
                        </div>
                    </t>
                    <t t-else="">
                        <div class="text-center py-5">
                            <i class="fa fa-box-open fa-4x text-muted mb-3"/>
                            <p class="text-muted">No products available at the moment.</p>
                        </div>
                    </t>
                </div>
            </section>
        </xpath>
    </template>
    
    <!-- ============================================================ -->
    <!-- SHOP PAGE - SMART FILTERS (Adds to existing aside if present, otherwise creates one) -->
    <!-- ============================================================ -->
    
    <!-- ============================================================ -->
    <!-- SHOP PAGE - FEATURED STORES SECTION -->
    <!-- ============================================================ -->
    
    <!-- Featured Stores Horizontal Section - Enhanced UI/UX -->
    <template id="shop_featured_stores" inherit_id="website_sale.products" name="SMART Featured Stores Section">
        <xpath expr="//div[hasclass('o_wsale_products_main_row')]" position="before">
            <t t-if="active_sellers">
                <!-- Featured Stores Section with improved accessibility -->
                <section class="smart-shop-stores py-4 mb-4" 
                         role="region" 
                         aria-label="Shop by Store"
                         style="background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%); border-radius: 16px;">
                    <div class="container-fluid px-4">
                        <!-- Section Header with better semantics -->
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h2 class="h5 fw-bold mb-0" style="color: #714B67;">
                                <i class="fa fa-store me-2" aria-hidden="true"/>Shop by Store
                            </h2>
                            <a href="/stores" class="btn btn-sm btn-outline-primary" aria-label="View all stores">
                                View All <span class="d-none d-sm-inline">Stores</span> <i class="fa fa-arrow-right ms-1" aria-hidden="true"/>
                            </a>
                        </div>
                        
                        <!-- Stores Horizontal Scroll with keyboard navigation -->
                        <div class="smart-stores-scroll position-relative" role="list" aria-label="Available stores">
                            <div class="d-flex gap-3 overflow-auto pb-2 smart-stores-container" 
                                 style="scroll-snap-type: x mandatory; -webkit-overflow-scrolling: touch; scroll-behavior: smooth;">
                                <!-- All Stores Card -->
                                <a t-att-href="keep(seller_id='')" 
                                   role="listitem"
                                   aria-label="Show products from all stores"
                                   t-attf-class="smart-store-card flex-shrink-0 text-decoration-none #{'active-store' if not selected_seller else ''}"
                                   style="scroll-snap-align: start; width: 130px;">
                                    <div t-attf-class="card h-100 border-0 shadow-sm text-center smart-store-card-inner #{'border-primary border-2' if not selected_seller else ''}">
                                        <div class="card-body p-2 p-sm-3">
                                            <div class="rounded-circle mx-auto mb-2 d-flex align-items-center justify-content-center smart-store-icon"
                                                 t-attf-style="width: 56px; height: 56px; background: #{not selected_seller and 'linear-gradient(135deg, #714B67 0%, #9B6A8F 100%)' or '#f0f0f0'};">
                                                <i t-attf-class="fa fa-th-large #{not selected_seller and 'text-white' or 'text-muted'}" aria-hidden="true"/>
                                            </div>
                                            <h3 class="card-title mb-1 small fw-bold text-truncate h6">All Stores</h3>
                                            <span class="badge bg-secondary small" aria-label="Store count">
                                                <t t-esc="len(active_sellers)"/> stores
                                            </span>
                                        </div>
                                    </div>
                                </a>
                                
                                <!-- Individual Store Cards -->
                                <t t-foreach="active_sellers[:12]" t-as="store">
                                    <a t-att-href="keep(seller_id=store.id)" 
                                       role="listitem"
                                       t-att-aria-label="'Shop from ' + store.company_name"
                                       t-att-aria-current="'true' if selected_seller and selected_seller.id == store.id else 'false'"
                                       t-attf-class="smart-store-card flex-shrink-0 text-decoration-none #{'active-store' if selected_seller and selected_seller.id == store.id else ''}"
                                       style="scroll-snap-align: start; width: 130px;">
                                        <div t-attf-class="card h-100 border-0 shadow-sm text-center smart-store-card-inner #{'border-primary border-2' if selected_seller and selected_seller.id == store.id else ''}">
                                            <div class="card-body p-2 p-sm-3">
                                                <t t-if="store.store_logo">
                                                    <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % store.id" 
                                                         t-att-alt="'%s store logo' % store.company_name"
                                                         class="rounded-circle mx-auto mb-2 smart-store-logo" 
                                                         style="width: 56px; height: 56px; object-fit: cover; border: 2px solid #eee;"
                                                         loading="lazy"/>
                                                </t>
                                                <t t-else="">
                                                    <div class="rounded-circle mx-auto mb-2 d-flex align-items-center justify-content-center bg-light smart-store-icon"
                                                         style="width: 56px; height: 56px;" aria-hidden="true">
                                                        <i class="fa fa-store text-muted"/>
                                                    </div>
                                                </t>
                                                <h3 class="card-title mb-1 small fw-bold text-truncate h6" t-att-title="store.company_name">
                                                    <t t-esc="store.company_name"/>
                                                </h3>
                                                <span class="badge bg-primary small">
                                                    <t t-esc="store.product_count"/> <span class="visually-hidden">products</span>
                                                </span>
                                                <t t-if="store.rating > 0">
                                                    <span class="badge bg-warning text-dark small ms-1" t-att-aria-label="'Rating: ' + str(store.rating)">
                                                        <i class="fa fa-star" aria-hidden="true"/> <t t-esc="'%.1f' % store.rating"/>
                                                    </span>
                                                </t>
                                            </div>
                                        </div>
                                    </a>
                                </t>
                            </div>
                            
                            <!-- Scroll Indicators for better UX -->
                            <div class="smart-scroll-indicators d-none d-md-flex justify-content-center mt-2 gap-1" aria-hidden="true">
                                <span class="smart-scroll-dot active"></span>
                                <span class="smart-scroll-dot"></span>
                                <span class="smart-scroll-dot"></span>
                            </div>
                        </div>
                        
                        <!-- Selected Store Info Banner - Enhanced -->
                        <div t-if="selected_seller" 
                             class="smart-selected-store-banner alert alert-info d-flex flex-column flex-md-row align-items-start align-items-md-center justify-content-between mt-3 mb-0 border-0" 
                             role="status"
                             aria-live="polite"
                             style="background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%); border-radius: 12px;">
                            <div class="d-flex align-items-center mb-2 mb-md-0">
                                <t t-if="selected_seller.store_logo">
                                    <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % selected_seller.id" 
                                         t-att-alt="selected_seller.company_name"
                                         class="rounded-circle me-3 shadow-sm" 
                                         style="width: 48px; height: 48px; object-fit: cover; border: 2px solid white;"/>
                                </t>
                                <t t-else="">
                                    <div class="rounded-circle me-3 d-flex align-items-center justify-content-center bg-white shadow-sm"
                                         style="width: 48px; height: 48px;" aria-hidden="true">
                                        <i class="fa fa-store fa-lg" style="color: #714B67;"/>
                                    </div>
                                </t>
                                <div>
                                    <strong class="d-block">
                                        <i class="fa fa-filter me-1 text-primary" aria-hidden="true"/>
                                        Showing: <t t-esc="selected_seller.company_name"/>
                                    </strong>
                                    <div class="small text-muted">
                                        <span class="me-2"><i class="fa fa-cubes me-1" aria-hidden="true"/><t t-esc="selected_seller.product_count"/> products</span>
                                        <t t-if="selected_seller.rating > 0">
                                            <span><i class="fa fa-star text-warning me-1" aria-hidden="true"/><t t-esc="'%.1f' % selected_seller.rating"/> rating</span>
                                        </t>
                                    </div>
                                </div>
                            </div>
                            <div class="d-flex gap-2 flex-wrap">
                                <a t-att-href="'/store/%s' % slug(selected_seller)" 
                                   class="btn btn-sm btn-primary"
                                   aria-label="Visit store page">
                                    <i class="fa fa-external-link me-1" aria-hidden="true"/>Visit Store
                                </a>
                                <a t-att-href="keep(seller_id='')" 
                                   class="btn btn-sm btn-outline-secondary"
                                   aria-label="Clear store filter and show all products">
                                    <i class="fa fa-times me-1" aria-hidden="true"/>Show All
                                </a>
                            </div>
                        </div>
                    </div>
                </section>
            </t>
        </xpath>
    </template>
    
    <!-- Mobile Filter Button (above the main row) -->
    <template id="shop_mobile_filter_button" inherit_id="website_sale.products" name="SMART Mobile Filter Button">
        <xpath expr="//div[hasclass('o_wsale_products_main_row')]" position="before">
            <!-- Mobile Filter Toggle Button -->
            <div class="d-lg-none mb-3 px-3">
                <button class="btn btn-outline-primary w-100" type="button" 
                        data-bs-toggle="offcanvas" data-bs-target="#smartMobileFilters" 
                        aria-controls="smartMobileFilters">
                    <i class="fa fa-filter me-2"/>Filters &amp; Categories
                </button>
            </div>
            
            <!-- Mobile Offcanvas Filters -->
            <div class="offcanvas offcanvas-start" tabindex="-1" id="smartMobileFilters" aria-labelledby="smartMobileFiltersLabel">
                <div class="offcanvas-header" style="background: linear-gradient(135deg, #714B67 0%, #9B6A8F 100%);">
                    <h5 class="offcanvas-title text-white" id="smartMobileFiltersLabel">
                        <i class="fa fa-filter me-2"/>Filters
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas" aria-label="Close"/>
                </div>
                <div class="offcanvas-body">
                    <!-- Categories -->
                    <div class="mb-4">
                        <h6 class="fw-bold border-bottom pb-2" style="color: #714B67;">
                            <i class="fa fa-th-list me-2"/>Categories
                        </h6>
                        <div class="d-flex flex-wrap gap-2">
                            <a href="/shop" t-attf-class="btn btn-sm #{'btn-primary' if not category else 'btn-outline-secondary'}">
                                <i class="fa fa-home me-1"/>All
                            </a>
                            <t t-foreach="categories" t-as="c">
                                <a t-att-href="keep('/shop/category/%s' % slug(c), category=0)" 
                                   t-attf-class="btn btn-sm #{'btn-primary' if category and category.id == c.id else 'btn-outline-secondary'}">
                                    <t t-esc="c.name"/>
                                </a>
                            </t>
                        </div>
                    </div>
                    
                    <!-- Availability -->
                    <div class="mb-4">
                        <h6 class="fw-bold border-bottom pb-2" style="color: #714B67;">
                            <i class="fa fa-check-circle me-2"/>Availability
                        </h6>
                        <div class="d-flex flex-wrap gap-2">
                            <a t-att-href="keep(availability='')" 
                               t-attf-class="btn btn-sm #{'btn-secondary' if not selected_availability else 'btn-outline-secondary'}">All</a>
                            <a t-att-href="keep(availability='in_stock')" 
                               t-attf-class="btn btn-sm #{'btn-success' if selected_availability == 'in_stock' else 'btn-outline-success'}">
                                <i class="fa fa-check me-1"/>In Stock
                            </a>
                            <a t-att-href="keep(availability='low_stock')" 
                               t-attf-class="btn btn-sm #{'btn-warning' if selected_availability == 'low_stock' else 'btn-outline-warning'}">
                                <i class="fa fa-exclamation-triangle me-1"/>Low Stock
                            </a>
                            <a t-att-href="keep(availability='out_of_stock')" 
                               t-attf-class="btn btn-sm #{'btn-danger' if selected_availability == 'out_of_stock' else 'btn-outline-danger'}">
                                <i class="fa fa-times me-1"/>Out of Stock
                            </a>
                        </div>
                    </div>
                    
                    <!-- Store/Seller Filter - Enhanced for Mobile -->
                    <t t-if="active_sellers">
                        <div class="mb-4">
                            <h6 class="fw-bold border-bottom pb-2" style="color: #714B67;">
                                <i class="fa fa-store me-2"/>Filter by Store
                            </h6>
                            <div class="smart-store-filter-mobile" style="max-height: 250px; overflow-y: auto;">
                                <!-- All Stores Option -->
                                <a t-att-href="keep(seller_id='')" 
                                   t-attf-class="d-flex align-items-center p-2 mb-2 rounded text-decoration-none #{'bg-primary text-white' if not selected_seller else 'bg-light text-dark'}">
                                    <div class="rounded-circle d-flex align-items-center justify-content-center me-2"
                                         t-attf-style="width: 40px; height: 40px; background: #{not selected_seller and 'rgba(255,255,255,0.2)' or '#714B67'};">
                                        <i t-attf-class="fa fa-th-large #{not selected_seller and 'text-white' or 'text-white'}"/>
                                    </div>
                                    <div class="flex-grow-1">
                                        <div class="fw-bold">All Stores</div>
                                        <small t-att-style="not selected_seller and 'opacity: 0.8;' or 'color: #6c757d;'">
                                            <t t-esc="len(active_sellers)"/> stores available
                                        </small>
                                    </div>
                                    <t t-if="not selected_seller">
                                        <i class="fa fa-check-circle"/>
                                    </t>
                                </a>
                                
                                <!-- Individual Stores -->
                                <t t-foreach="active_sellers" t-as="s">
                                    <a t-att-href="keep(seller_id=s.id)" 
                                       t-attf-class="d-flex align-items-center p-2 mb-2 rounded text-decoration-none #{'bg-primary text-white' if selected_seller and selected_seller.id == s.id else 'bg-light text-dark'}">
                                        <t t-if="s.store_logo">
                                            <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % s.id" 
                                                 t-att-alt="s.company_name"
                                                 class="rounded-circle me-2" 
                                                 style="width: 40px; height: 40px; object-fit: cover; border: 2px solid #eee;"/>
                                        </t>
                                        <t t-else="">
                                            <div class="rounded-circle d-flex align-items-center justify-content-center me-2 bg-secondary"
                                                 style="width: 40px; height: 40px;">
                                                <i class="fa fa-store text-white"/>
                                            </div>
                                        </t>
                                        <div class="flex-grow-1">
                                            <div class="fw-bold text-truncate" style="max-width: 180px;" t-att-title="s.company_name">
                                                <t t-esc="s.company_name"/>
                                            </div>
                                            <small t-att-style="(selected_seller and selected_seller.id == s.id) and 'opacity: 0.8;' or 'color: #6c757d;'">
                                                <t t-esc="s.product_count"/> products
                                                <t t-if="s.rating > 0">
                                                    • <i class="fa fa-star text-warning"/> <t t-esc="'%.1f' % s.rating"/>
                                                </t>
                                            </small>
                                        </div>
                                        <t t-if="selected_seller and selected_seller.id == s.id">
                                            <i class="fa fa-check-circle"/>
                                        </t>
                                    </a>
                                </t>
                            </div>
                            <!-- View All Stores Button -->
                            <a href="/stores" class="btn btn-outline-primary w-100 mt-2">
                                <i class="fa fa-external-link me-1"/>Browse All Stores
                            </a>
                        </div>
                    </t>
                    
                    <!-- Brand Filter -->
                    <t t-if="brands">
                        <div class="mb-4">
                            <h6 class="fw-bold border-bottom pb-2" style="color: #714B67;">
                                <i class="fa fa-tag me-2"/>Brand
                            </h6>
                            <div class="d-flex flex-wrap gap-2">
                                <a t-att-href="keep(brand='')" 
                                   t-attf-class="btn btn-sm #{'btn-secondary' if not selected_brand else 'btn-outline-secondary'}">All</a>
                                <t t-foreach="brands" t-as="b">
                                    <a t-att-href="keep(brand=b)" 
                                       t-attf-class="btn btn-sm #{'btn-primary' if selected_brand == b else 'btn-outline-primary'}">
                                        <t t-esc="b"/>
                                    </a>
                                </t>
                            </div>
                        </div>
                    </t>
                    
                    <!-- Price Range -->
                    <div class="mb-4">
                        <h6 class="fw-bold border-bottom pb-2" style="color: #714B67;">
                            <i class="fa fa-money me-2"/>Price Range
                        </h6>
                        <form method="get" class="row g-2">
                            <div class="col-5">
                                <input type="number" class="form-control form-control-sm" name="price_min" placeholder="Min" t-att-value="selected_price_min"/>
                            </div>
                            <div class="col-2 text-center align-self-center">-</div>
                            <div class="col-5">
                                <input type="number" class="form-control form-control-sm" name="price_max" placeholder="Max" t-att-value="selected_price_max"/>
                            </div>
                            <div class="col-12 mt-2">
                                <button type="submit" class="btn btn-primary btn-sm w-100">
                                    <i class="fa fa-search me-1"/>Apply
                                </button>
                            </div>
                        </form>
                    </div>
                    
                    <!-- Clear Filters -->
                    <div t-if="category or selected_brand or selected_availability" class="mt-4">
                        <a href="/shop" class="btn btn-outline-danger w-100">
                            <i class="fa fa-times me-1"/>Clear All Filters
                        </a>
                    </div>
                </div>
            </div>
            
            <!-- Active Filters Display -->
            <div t-if="category or selected_seller or selected_brand or selected_availability or selected_price_min or selected_price_max" 
                 class="px-3 mb-3">
                <div class="d-flex flex-wrap gap-2 align-items-center p-2 bg-light rounded">
                    <span class="text-muted small fw-bold">Active:</span>
                    <t t-if="category">
                        <a href="/shop" class="badge bg-primary text-decoration-none">
                            <t t-esc="category.name"/> <i class="fa fa-times ms-1"/>
                        </a>
                    </t>
                    <t t-if="selected_seller">
                        <a t-att-href="keep(seller_id='')" class="badge bg-info text-decoration-none">
                            <i class="fa fa-store me-1"/><t t-esc="selected_seller.company_name"/> <i class="fa fa-times ms-1"/>
                        </a>
                    </t>
                    <t t-if="selected_brand">
                        <a t-att-href="keep(brand='')" class="badge bg-info text-decoration-none">
                            <t t-esc="selected_brand"/> <i class="fa fa-times ms-1"/>
                        </a>
                    </t>
                    <t t-if="selected_availability">
                        <a t-att-href="keep(availability='')" class="badge bg-success text-decoration-none">
                            <t t-esc="selected_availability.replace('_', ' ').title()"/> <i class="fa fa-times ms-1"/>
                        </a>
                    </t>
                    <a href="/shop" class="badge bg-danger text-decoration-none ms-auto">
                        <i class="fa fa-refresh me-1"/>Reset
                    </a>
                </div>
            </div>
        </xpath>
    </template>
    
    <!-- Desktop Filter Sidebar (in the existing aside element) -->
    <template id="shop_desktop_filters" inherit_id="website_sale.products" name="SMART Desktop Filters">
        <xpath expr="//aside[@id='products_grid_before']//div[hasclass('o_wsale_products_grid_before_rail')]" position="inside">
            <!-- SMART Additional Filters -->
            <div class="smart-extra-filters mt-4 pt-3 border-top">
                <!-- Availability Filter -->
                <div class="mb-4">
                    <h6 class="fw-bold small mb-3" style="color: #714B67;">
                        <i class="fa fa-check-circle me-2"/>Availability
                    </h6>
                    <div class="d-flex flex-column gap-1">
                        <a t-att-href="keep(availability='')" 
                           t-attf-class="small text-decoration-none #{'fw-bold text-primary' if not selected_availability else 'text-dark'}">
                            <i class="fa fa-circle-o me-1"/> All
                        </a>
                        <a t-att-href="keep(availability='in_stock')" 
                           t-attf-class="small text-decoration-none #{'fw-bold text-success' if selected_availability == 'in_stock' else 'text-success'}">
                            <i class="fa fa-check me-1"/> In Stock
                        </a>
                        <a t-att-href="keep(availability='low_stock')" 
                           t-attf-class="small text-decoration-none #{'fw-bold text-warning' if selected_availability == 'low_stock' else 'text-warning'}">
                            <i class="fa fa-exclamation-triangle me-1"/> Low Stock
                        </a>
                        <a t-att-href="keep(availability='out_of_stock')" 
                           t-attf-class="small text-decoration-none #{'fw-bold text-danger' if selected_availability == 'out_of_stock' else 'text-danger'}">
                            <i class="fa fa-times me-1"/> Out of Stock
                        </a>
                    </div>
                </div>
                
                <!-- Store/Seller Filter - Enhanced -->
                <t t-if="active_sellers">
                    <div class="mb-4">
                        <h6 class="fw-bold small mb-3" style="color: #714B67;">
                            <i class="fa fa-store me-2"/>Filter by Store
                        </h6>
                        <div class="smart-store-filter-list" style="max-height: 300px; overflow-y: auto;">
                            <!-- All Stores Option -->
                            <a t-att-href="keep(seller_id='')" 
                               t-attf-class="d-flex align-items-center p-2 mb-1 rounded text-decoration-none #{'bg-primary text-white' if not selected_seller else 'bg-light text-dark'}"
                               style="transition: background 0.2s;">
                                <div class="rounded-circle d-flex align-items-center justify-content-center me-2"
                                     t-attf-style="width: 32px; height: 32px; background: #{not selected_seller and 'rgba(255,255,255,0.2)' or '#714B67'};">
                                    <i t-attf-class="fa fa-th-large small #{not selected_seller and 'text-white' or 'text-white'}"/>
                                </div>
                                <div class="flex-grow-1 min-width-0">
                                    <div class="small fw-bold text-truncate">All Stores</div>
                                    <div class="small" t-att-style="not selected_seller and 'opacity: 0.8;' or 'color: #6c757d;'">
                                        <t t-esc="len(active_sellers)"/> stores
                                    </div>
                                </div>
                            </a>
                            
                            <!-- Individual Stores -->
                            <t t-foreach="active_sellers" t-as="s">
                                <a t-att-href="keep(seller_id=s.id)" 
                                   t-attf-class="d-flex align-items-center p-2 mb-1 rounded text-decoration-none #{'bg-primary text-white' if selected_seller and selected_seller.id == s.id else 'bg-light text-dark'}"
                                   style="transition: background 0.2s;">
                                    <t t-if="s.store_logo">
                                        <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % s.id" 
                                             t-att-alt="s.company_name"
                                             class="rounded-circle me-2" 
                                             style="width: 32px; height: 32px; object-fit: cover; border: 1px solid #eee;"/>
                                    </t>
                                    <t t-else="">
                                        <div class="rounded-circle d-flex align-items-center justify-content-center me-2 bg-secondary"
                                             style="width: 32px; height: 32px;">
                                            <i class="fa fa-store small text-white"/>
                                        </div>
                                    </t>
                                    <div class="flex-grow-1 min-width-0">
                                        <div class="small fw-bold text-truncate" t-att-title="s.company_name">
                                            <t t-esc="s.company_name"/>
                                        </div>
                                        <div class="small" t-att-style="(selected_seller and selected_seller.id == s.id) and 'opacity: 0.8;' or 'color: #6c757d;'">
                                            <t t-esc="s.product_count"/> products
                                            <t t-if="s.rating > 0">
                                                • <i class="fa fa-star text-warning"/> <t t-esc="'%.1f' % s.rating"/>
                                            </t>
                                        </div>
                                    </div>
                                </a>
                            </t>
                        </div>
                        <!-- View All Stores Link -->
                        <a href="/stores" class="btn btn-sm btn-outline-primary w-100 mt-2">
                            <i class="fa fa-external-link me-1"/>Browse All Stores
                        </a>
                    </div>
                </t>
                
                <!-- Brand Filter -->
                <t t-if="brands">
                    <div class="mb-4">
                        <h6 class="fw-bold small mb-3" style="color: #714B67;">
                            <i class="fa fa-tag me-2"/>Brand
                        </h6>
                        <div class="d-flex flex-wrap gap-1">
                            <a t-att-href="keep(brand='')" 
                               t-attf-class="badge text-decoration-none #{'bg-secondary' if not selected_brand else 'bg-light text-dark border'}">
                                All
                            </a>
                            <t t-foreach="brands" t-as="b">
                                <a t-att-href="keep(brand=b)" 
                                   t-attf-class="badge text-decoration-none #{'bg-primary' if selected_brand == b else 'bg-light text-dark border'}">
                                    <t t-esc="b"/>
                                </a>
                            </t>
                        </div>
                    </div>
                </t>
            </div>
        </xpath>
    </template>
    
    <!-- ============================================================ -->
    <!-- CHECKOUT - DELIVERY ZONE SELECTION -->
    <!-- ============================================================ -->
    
    <template id="checkout_delivery_zone" inherit_id="website_sale.checkout" name="Checkout Delivery Zone">
        <xpath expr="//div[@id='shop_checkout']" position="inside">
            <t t-set="delivery_zones" t-value="request.env['delivery.zone'].sudo().search([('active', '=', True)])"/>
            <t t-set="error" t-value="request.params.get('error')"/>
            
            <!-- Error message for missing delivery zone -->
            <t t-if="error == 'delivery_zone_required' and website_sale_order and website_sale_order._has_deliverable_products()">
                <div class="alert alert-warning alert-dismissible fade show mt-3" role="alert">
                    <i class="fa fa-exclamation-triangle me-2"/>Please select a delivery zone before proceeding to payment.
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            </t>
            
            <!-- Use div.smart-zone-panel instead of div.card to avoid triggering address change handlers -->
            <div t-if="delivery_zones" class="smart-delivery-zone-section mt-4 mb-4">
                <div class="smart-zone-panel border-0 shadow-sm rounded overflow-hidden">
                    <div class="smart-zone-header text-white p-3" style="background: linear-gradient(135deg, #714B67 0%, #9B6A8F 100%);">
                        <h5 class="mb-0">
                            <i class="fa fa-truck me-2"/>Select Delivery Zone
                            <t t-if="website_sale_order and website_sale_order._has_deliverable_products() and not website_sale_order.delivery_zone_id">
                                <span class="badge bg-warning text-dark ms-2">Required</span>
                            </t>
                        </h5>
                    </div>
                    <div class="smart-zone-body p-3 bg-white">
                        <p class="text-muted mb-3">
                            <t t-if="website_sale_order and website_sale_order.partner_shipping_id and website_sale_order.partner_shipping_id.city">
                                <i class="fa fa-map-marker me-1 text-primary"/>Shipping to: <strong t-esc="website_sale_order.partner_shipping_id.city"/>
                            </t>
                            <t t-else="">
                                Choose your delivery zone to see accurate delivery costs and estimated delivery times.
                            </t>
                        </p>
                        
                        <div class="delivery-zone-options">
                            <t t-foreach="delivery_zones" t-as="zone">
                                <div class="form-check delivery-zone-option mb-3 p-3 border rounded"
                                     t-att-data-zone-id="zone.id"
                                     t-att-data-zone-price="zone.base_price"
                                     t-att-data-zone-days="zone.estimated_days"
                                     t-att-data-zone-name="zone.name">
                                    <input class="form-check-input delivery-zone-radio" type="radio" 
                                           name="delivery_zone_id" t-att-id="'zone_%s' % zone.id"
                                           t-att-value="zone.id"
                                           t-att-checked="order.delivery_zone_id.id == zone.id if order else False"/>
                                    <label class="form-check-label w-100" t-att-for="'zone_%s' % zone.id" style="cursor: pointer;">
                                        <div class="d-flex justify-content-between align-items-center">
                                            <div>
                                                <strong><t t-esc="zone.name"/></strong>
                                                <div class="text-muted small">
                                                    <i class="fa fa-clock-o me-1"/>Est: <t t-esc="zone.estimated_days"/> day<t t-if="zone.estimated_days != 1">s</t>
                                                </div>
                                                <t t-if="zone.free_delivery_threshold > 0">
                                                    <div class="text-success small">
                                                        <i class="fa fa-gift me-1"/>Free delivery over <t t-esc="zone.free_delivery_threshold" t-options="{'widget': 'monetary', 'display_currency': zone.currency_id}"/>
                                                    </div>
                                                </t>
                                            </div>
                                            <span class="badge fs-6" style="background: linear-gradient(135deg, #28a745 0%, #218838 100%);">
                                                <t t-esc="zone.base_price" t-options="{'widget': 'monetary', 'display_currency': zone.currency_id}"/>
                                            </span>
                                        </div>
                                    </label>
                                </div>
                            </t>
                        </div>
                        
                        <!-- Selected zone info display -->
                        <div id="delivery_zone_info" class="mt-3 p-3 bg-light rounded" t-att-class="'d-none' if not (website_sale_order and website_sale_order.delivery_zone_id) else ''">
                            <div class="d-flex justify-content-between align-items-center flex-wrap">
                                <div class="mb-2 mb-md-0">
                                    <strong><i class="fa fa-check-circle me-1 text-success"/>Selected Zone: </strong>
                                    <span id="selected_zone_name" t-esc="website_sale_order.delivery_zone_id.name if website_sale_order and website_sale_order.delivery_zone_id else ''"/>
                                </div>
                                <div class="d-flex gap-2 flex-wrap">
                                    <span class="badge bg-info">
                                        <i class="fa fa-clock-o me-1"/>Est. <span id="selected_zone_days">
                                            <t t-if="website_sale_order and website_sale_order.delivery_zone_id">
                                                <t t-esc="website_sale_order.delivery_zone_id.estimated_days"/> day<t t-if="website_sale_order.delivery_zone_id.estimated_days != 1">s</t>
                                            </t>
                                        </span>
                                    </span>
                                    <span class="badge bg-success" id="selected_zone_price">
                                        <t t-if="website_sale_order and website_sale_order.computed_delivery_price">
                                            <t t-esc="website_sale_order.computed_delivery_price" t-options="{'widget': 'monetary', 'display_currency': website_sale_order.currency_id}"/>
                                        </t>
                                        <t t-else="">
                                            -
                                        </t>
                                    </span>
                                </div>
                            </div>
                            <t t-if="website_sale_order and website_sale_order.estimated_delivery_date">
                                <div class="mt-2 small text-muted">
                                    <i class="fa fa-calendar me-1"/>Estimated delivery: 
                                    <strong t-esc="website_sale_order.estimated_delivery_date" t-options="{'widget': 'date'}"/>
                                </div>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
    
    <!-- ============================================================ -->
    <!-- PRODUCT PAGE ENHANCEMENTS -->
    <!-- ============================================================ -->
    
    <template id="product_brand_model" inherit_id="website_sale.product" name="Product Brand &amp; Model">
        <xpath expr="//h1[@itemprop='name']" position="after">
            <div t-if="product.brand or product.product_model" class="product-brand-model mb-2">
                <span t-if="product.brand" class="badge text-bg-secondary me-1"><t t-esc="product.brand"/></span>
                <span t-if="product.product_model" class="text-muted">Model: <t t-esc="product.product_model"/></span>
            </div>
            <div class="mb-2">
                <span t-if="product.availability_status == 'in_stock'" class="badge text-bg-success">
                    <i class="fa fa-check me-1"/> In Stock
                </span>
                <span t-elif="product.availability_status == 'low_stock'" class="badge text-bg-warning">
                    <i class="fa fa-exclamation-triangle me-1"/> Low Stock
                </span>
                <span t-elif="product.availability_status == 'out_of_stock'" class="badge text-bg-danger">
                    <i class="fa fa-times me-1"/> Out of Stock
                </span>
            </div>
            <!-- Seller/Store Info -->
            <div t-if="product.seller_id and product.seller_id.can_do_commercial_actions" class="mb-3">
                <div class="d-flex align-items-center">
                    <t t-if="product.seller_id.store_logo">
                        <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % product.seller_id.id" 
                             class="rounded-circle me-2" 
                             style="width: 32px; height: 32px; object-fit: cover;"/>
                    </t>
                    <t t-else="">
                        <i class="fa fa-store me-2 text-muted"/>
                    </t>
                    <div>
                        <small class="text-muted d-block">Sold by</small>
                        <a t-att-href="'/store/%s' % slug(product.seller_id)" 
                           class="text-decoration-none fw-bold" 
                           style="color: #714B67;">
                            <t t-esc="product.seller_id.company_name"/>
                        </a>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
    
    <template id="product_specifications_video" inherit_id="website_sale.product" name="Specifications &amp; Video">
        <xpath expr="//div[@id='product_full_description']" position="after">
            <div t-if="product.specifications" class="mt-4">
                <h4 class="border-bottom pb-2"><i class="fa fa-list-ul me-2"/>Specifications</h4>
                <div class="smart-specifications" t-out="product.specifications"/>
            </div>
            <div t-if="product.video_url" class="mt-4 smart-product-video">
                <h4 class="border-bottom pb-2"><i class="fa fa-play-circle me-2"/>Product Video</h4>
                <div class="ratio ratio-16x9">
                    <iframe t-att-src="product.get_video_embed_url()" frameborder="0" allowfullscreen="allowfullscreen"/>
                </div>
            </div>
        </xpath>
    </template>
    
    <!-- ============================================================ -->
    <!-- ORDER CONFIRMATION PAGE -->
    <!-- ============================================================ -->
    
    <template id="order_confirmation_delivery" inherit_id="website_sale.confirmation" name="Order Confirmation Delivery Info">
        <xpath expr="//div[@id='oe_structure_website_sale_confirmation_1']" position="before">
            <div class="alert alert-success d-flex align-items-center mt-3">
                <i class="fa fa-check-circle fa-2x me-3 text-success"/>
                <div>
                    <strong>Your order has been confirmed!</strong>
                    <p class="mb-0 small">Order reference: <strong t-field="order.name"/></p>
                </div>
            </div>
            
            <div t-if="order.delivery_zone_id" class="card mt-3 border-info">
                <div class="card-body">
                    <h5 class="card-title"><i class="fa fa-truck me-2 text-info"/>Delivery Information</h5>
                    <div class="row">
                        <div class="col-md-6">
                            <p class="mb-1"><strong>Delivery Zone:</strong> <t t-esc="order.delivery_zone_id.name"/></p>
                            <p class="mb-1" t-if="order.estimated_delivery_date">
                                <strong>Estimated Delivery:</strong> 
                                <t t-esc="order.estimated_delivery_date" t-options="{'widget': 'date'}"/>
                            </p>
                        </div>
                        <div class="col-md-6">
                            <p class="mb-1" t-if="order.computed_delivery_price">
                                <strong>Delivery Cost:</strong> 
                                <t t-esc="order.computed_delivery_price" t-options="{'widget': 'monetary', 'display_currency': order.currency_id}"/>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
    
    <!-- ============================================================ -->
    <!-- STORES LISTING PAGE -->
    <!-- ============================================================ -->
    
    <template id="stores_list" name="Stores List">
        <t t-call="website.layout">
            <t t-set="title">Stores</t>
            <div id="wrap" class="oe_structure oe_empty">
                <div class="container py-5">
                    <!-- Header -->
                    <div class="text-center mb-5">
                        <h1 class="display-4 fw-bold" style="color:rgb(75, 113, 95);">
                            <i class="fa fa-store me-2"/>Our Stores
                        </h1>
                        <p class="text-muted lead">Discover amazing products from our trusted sellers</p>
                    </div>
                    
                    <!-- Search Bar -->
                    <div class="row mb-4">
                        <div class="col-md-8 offset-md-2">
                            <form method="get" action="/stores" class="d-flex">
                                <input type="text" name="search" class="form-control form-control-lg me-2" 
                                       placeholder="Search stores..." t-att-value="search"/>
                                <button type="submit" class="btn btn-primary btn-lg">
                                    <i class="fa fa-search me-2"/>Search
                                </button>
                            </form>
                        </div>
                    </div>
                    
                    <!-- Stores Grid -->
                    <t t-if="stores">
                        <div class="row g-4">
                            <t t-foreach="stores" t-as="store">
                                <div class="col-md-6 col-lg-4">
                                    <div class="card h-100 border-0 shadow-sm store-card" 
                                         style="transition: transform 0.2s; cursor: pointer;" 
                                         onmouseover="this.style.transform='translateY(-5px)'" 
                                         onmouseout="this.style.transform='translateY(0)'">
                                        <a t-att-href="'/store/%s' % slug(store)" class="text-decoration-none">
                                            <!-- Store Banner/Logo -->
                                            <div class="position-relative" style="height: 200px; background: linear-gradient(135deg, #714B67 0%, #9B6A8F 100%); overflow: hidden;">
                                                <t t-if="store.store_banner">
                                                    <img t-att-src="'/web/image/marketplace.seller/%s/store_banner' % store.id" 
                                                         class="w-100 h-100" style="object-fit: cover;"/>
                                                </t>
                                                <t t-else="">
                                                    <div class="d-flex align-items-center justify-content-center h-100">
                                                        <i class="fa fa-store fa-4x text-white opacity-50"/>
                                                    </div>
                                                </t>
                                                <!-- Store Logo Overlay -->
                                                <div class="position-absolute bottom-0 start-50 translate-middle-x mb-3">
                                                    <t t-if="store.store_logo">
                                                        <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % store.id" 
                                                             class="rounded-circle border border-4 border-white shadow" 
                                                             style="width: 80px; height: 80px; object-fit: cover;"/>
                                                    </t>
                                                    <t t-else="">
                                                        <div class="bg-white rounded-circle d-flex align-items-center justify-content-center shadow" 
                                                             style="width: 80px; height: 80px;">
                                                            <i class="fa fa-store fa-2x" style="color: #714B67;"/>
                                                        </div>
                                                    </t>
                                                </div>
                                            </div>
                                            
                                            <!-- Store Info -->
                                            <div class="card-body text-center pt-5">
                                                <h5 class="card-title mb-2">
                                                    <t t-esc="store.company_name"/>
                                                </h5>
                                                <t t-if="store.store_description">
                                                    <p class="text-muted small mb-3" style="min-height: 40px; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">
                                                        <t t-esc="store.store_description.replace('&lt;', '&amp;lt;').replace('&gt;', '&amp;gt;')"/>
                                                    </p>
                                                </t>
                                                <div class="d-flex justify-content-center gap-3 mb-3">
                                                    <span class="badge bg-primary">
                                                        <i class="fa fa-cubes me-1"/><t t-esc="store.product_count"/> products
                                                    </span>
                                                    <t t-if="store.rating > 0">
                                                        <span class="badge bg-warning text-dark">
                                                            <i class="fa fa-star me-1"/><t t-esc="'%.1f' % store.rating"/>
                                                        </span>
                                                    </t>
                                                </div>
                                                <a t-att-href="'/store/%s' % slug(store)" class="btn btn-outline-primary btn-sm">
                                                    Visit Store <i class="fa fa-arrow-right ms-1"/>
                                                </a>
                                            </div>
                                        </a>
                                    </div>
                                </div>
                            </t>
                        </div>
                        
                        <!-- Pagination -->
                        <t t-if="total_pages > 1">
                            <nav aria-label="Stores pagination" class="mt-5">
                                <ul class="pagination justify-content-center">
                                    <li class="page-item" t-att-class="'disabled' if page == 1 else ''">
                                        <a class="page-link" t-att-href="'/stores/page/%s' % (page - 1) if page > 1 else '#'">
                                            <i class="fa fa-chevron-left"/> Previous
                                        </a>
                                    </li>
                                    <t t-foreach="range(1, total_pages + 1)" t-as="p">
                                        <li class="page-item" t-att-class="'active' if p == page else ''">
                                            <a class="page-link" t-att-href="'/stores/page/%s' % p">
                                                <t t-esc="p"/>
                                            </a>
                                        </li>
                                    </t>
                                    <li class="page-item" t-att-class="'disabled' if page >= total_pages else ''">
                                        <a class="page-link" t-att-href="'/stores/page/%s' % (page + 1) if page &lt; total_pages else '#'">
                                            Next <i class="fa fa-chevron-right"/>
                                        </a>
                                    </li>
                                </ul>
                            </nav>
                        </t>
                    </t>
                    <t t-else="">
                        <div class="text-center py-5">
                            <i class="fa fa-store fa-4x text-muted mb-3"/>
                            <h3 class="text-muted">No stores found</h3>
                            <p class="text-muted">Try adjusting your search criteria.</p>
                        </div>
                    </t>
                </div>
            </div>
        </t>
    </template>
    
    <!-- ============================================================ -->
    <!-- INDIVIDUAL STORE PAGE -->
    <!-- ============================================================ -->
    
    <template id="store_page" name="Store Page">
        <t t-call="website.layout">
            <t t-set="title" t-esc="seller.company_name"/>
            <div id="wrap" class="oe_structure oe_empty">
                <div class="container py-4">
                    <!-- Store Header -->
                    <div class="row mb-4">
                        <div class="col-12">
                            <div class="card border-0 shadow-sm">
                                <!-- Store Banner -->
                                <div class="position-relative" style="height: 250px; background: linear-gradient(135deg, #714B67 0%, #9B6A8F 100%); overflow: hidden;">
                                    <t t-if="seller.store_banner">
                                        <img t-att-src="'/web/image/marketplace.seller/%s/store_banner' % seller.id" 
                                             class="w-100 h-100" style="object-fit: cover;"/>
                                    </t>
                                    <t t-else="">
                                        <div class="d-flex align-items-center justify-content-center h-100">
                                            <i class="fa fa-store fa-5x text-white opacity-50"/>
                                        </div>
                                    </t>
                                    <!-- Store Logo -->
                                    <div class="position-absolute bottom-0 start-0 ms-4 mb-3">
                                        <t t-if="seller.store_logo">
                                            <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % seller.id" 
                                                 class="rounded-circle border border-4 border-white shadow" 
                                                 style="width: 120px; height: 120px; object-fit: cover;"/>
                                        </t>
                                        <t t-else="">
                                            <div class="bg-white rounded-circle d-flex align-items-center justify-content-center shadow" 
                                                 style="width: 120px; height: 120px;">
                                                <i class="fa fa-store fa-3x" style="color: #714B67;"/>
                                            </div>
                                        </t>
                                    </div>
                                </div>
                                
                                <!-- Store Info -->
                                <div class="card-body pt-5">
                                    <div class="row">
                                        <div class="col-md-8">
                                            <h1 class="mb-2" t-esc="seller.company_name"/>
                                            <t t-if="seller.store_description">
                                                <div class="text-muted mb-3" t-out="seller.store_description"/>
                                            </t>
                                            <div class="d-flex gap-3 flex-wrap">
                                                <span class="badge bg-primary fs-6">
                                                    <i class="fa fa-cubes me-1"/><t t-esc="total_products"/> products
                                                </span>
                                                <t t-if="seller.rating > 0">
                                                    <span class="badge bg-warning text-dark fs-6">
                                                        <i class="fa fa-star me-1"/><t t-esc="'%.1f' % seller.rating"/> rating
                                                    </span>
                                                </t>
                                                <t t-if="seller.city">
                                                    <span class="badge bg-secondary fs-6">
                                                        <i class="fa fa-map-marker me-1"/><t t-esc="seller.city"/>
                                                    </span>
                                                </t>
                                            </div>
                                        </div>
                                        <div class="col-md-4 text-md-end mt-3 mt-md-0">
                                            <a href="/stores" class="btn btn-outline-secondary">
                                                <i class="fa fa-arrow-left me-2"/>Back to Stores
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Products Section -->
                    <div class="mb-4">
                        <h2 class="mb-4">
                            <i class="fa fa-cubes me-2"/>Products
                        </h2>
                        
                        <t t-if="products">
                            <div class="row g-4">
                                <t t-foreach="products" t-as="product">
                                    <div class="col-6 col-md-4 col-lg-3">
                                        <div class="card h-100 border-0 shadow-sm product-card" 
                                             style="transition: transform 0.2s; cursor: pointer;" 
                                             onmouseover="this.style.transform='translateY(-5px)'" 
                                             onmouseout="this.style.transform='translateY(0)'">
                                            <a t-att-href="product.website_url" class="text-decoration-none">
                                                <div class="position-relative overflow-hidden" style="height: 200px; background-color: #f8f9fa;">
                                                    <t t-if="product.image_1920">
                                                        <img t-att-src="'/web/image/product.template/%s/image_1920' % product.id" 
                                                             t-att-alt="product.name"
                                                             class="w-100 h-100" 
                                                             style="object-fit: cover;"/>
                                                    </t>
                                                    <t t-else="">
                                                        <div class="d-flex align-items-center justify-content-center h-100">
                                                            <i class="fa fa-image fa-3x text-muted"/>
                                                        </div>
                                                    </t>
                                                    <t t-if="product.availability_status == 'in_stock'">
                                                        <span class="badge bg-success position-absolute top-0 end-0 m-2">
                                                            <i class="fa fa-check-circle me-1"/>In Stock
                                                        </span>
                                                    </t>
                                                    <t t-elif="product.availability_status == 'low_stock'">
                                                        <span class="badge bg-warning position-absolute top-0 end-0 m-2">
                                                            <i class="fa fa-exclamation-triangle me-1"/>Low Stock
                                                        </span>
                                                    </t>
                                                    <t t-elif="product.availability_status == 'out_of_stock'">
                                                        <span class="badge bg-danger position-absolute top-0 end-0 m-2">
                                                            <i class="fa fa-times-circle me-1"/>Out of Stock
                                                        </span>
                                                    </t>
                                                </div>
                                                <div class="card-body">
                                                    <h6 class="card-title text-dark mb-2" style="min-height: 40px; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">
                                                        <t t-esc="product.name"/>
                                                    </h6>
                                                    <div class="d-flex justify-content-between align-items-center mb-2">
                                                        <span class="h6 mb-0 text-primary fw-bold">
                                                            <t t-esc="product.list_price" t-options="{'widget': 'monetary', 'display_currency': website.currency_id}"/>
                                                        </span>
                                                        <t t-if="product.brand">
                                                            <span class="badge bg-secondary">
                                                                <t t-esc="product.brand"/>
                                                            </span>
                                                        </t>
                                                    </div>
                                                </div>
                                            </a>
                                            <div class="card-footer bg-white border-0 pt-0">
                                                <form action="/shop/cart/update" method="post" class="d-grid">
                                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                    <input type="hidden" name="product_id" t-att-value="product.id"/>
                                                    <input type="hidden" name="add_qty" value="1"/>
                                                    <button type="submit" 
                                                            class="btn btn-primary w-100 btn-sm"
                                                            t-att-disabled="product.availability_status == 'out_of_stock'">
                                                        <i class="fa fa-shopping-cart me-2"/>Add to Cart
                                                    </button>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </t>
                            </div>
                            
                            <!-- Pagination -->
                            <t t-if="total_pages > 1">
                                <nav aria-label="Products pagination" class="mt-5">
                                    <ul class="pagination justify-content-center">
                                        <li class="page-item" t-att-class="'disabled' if page == 1 else ''">
                                            <a class="page-link" t-att-href="'/store/%s/page/%s' % (slug(seller), page - 1) if page > 1 else '#'">
                                                <i class="fa fa-chevron-left"/> Previous
                                            </a>
                                        </li>
                                        <t t-foreach="range(1, total_pages + 1)" t-as="p">
                                            <li class="page-item" t-att-class="'active' if p == page else ''">
                                                <a class="page-link" t-att-href="'/store/%s/page/%s' % (slug(seller), p)">
                                                    <t t-esc="p"/>
                                                </a>
                                            </li>
                                        </t>
                                        <li class="page-item" t-att-class="'disabled' if page >= total_pages else ''">
                                            <a class="page-link" t-att-href="'/store/%s/page/%s' % (slug(seller), page + 1) if page &lt; total_pages else '#'">
                                                Next <i class="fa fa-chevron-right"/>
                                            </a>
                                        </li>
                                    </ul>
                                </nav>
                            </t>
                        </t>
                        <t t-else="">
                            <div class="text-center py-5">
                                <i class="fa fa-box-open fa-4x text-muted mb-3"/>
                                <h3 class="text-muted">No products available</h3>
                                <p class="text-muted">This store doesn't have any products yet.</p>
                            </div>
                        </t>
                    </div>
                </div>
            </div>
        </t>
    </template>
    
    <!-- ============================================================ -->
    <!-- CART - GROUPED BY SELLER VIEW -->
    <!-- ============================================================ -->
    
    <!-- Multi-seller cart notice banner -->
    <template id="cart_lines_grouped_by_seller" inherit_id="website_sale.cart_lines" name="Cart Lines Grouped by Seller">
        <xpath expr="//div[@id='cart_products']" position="before">
            <!-- Multi-seller notice banner -->
            <t t-set="cart_sellers" t-value="website_sale_order.order_line.mapped('product_id.product_tmpl_id.seller_id').filtered(lambda s: s.id) if website_sale_order else []"/>
            <div t-if="cart_sellers and len(cart_sellers) > 1" class="alert border-0 mb-4 smart-multi-seller-notice" role="alert"
                 style="background: linear-gradient(135deg, #e8f4fd 0%, #d0e8f8 100%); border-radius: 12px;">
                <div class="d-flex align-items-start">
                    <i class="fa fa-info-circle fa-lg me-3 mt-1 text-primary" aria-hidden="true"/>
                    <div>
                        <strong class="d-block mb-1">
                            <i class="fa fa-store me-1" aria-hidden="true"/>Multiple Stores in Your Cart
                        </strong>
                        <p class="mb-2 small">
                            Your cart contains items from <strong><t t-esc="len(cart_sellers)"/> different stores</strong>. 
                            Each store will process and ship their items separately for faster delivery.
                        </p>
                        <div class="d-flex flex-wrap gap-2">
                            <t t-foreach="cart_sellers" t-as="cart_seller">
                                <span class="badge bg-white text-dark border shadow-sm">
                                    <t t-if="cart_seller.store_logo">
                                        <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % cart_seller.id" 
                                             class="rounded-circle me-1" style="width: 16px; height: 16px; object-fit: cover;"
                                             t-att-alt="cart_seller.company_name"/>
                                    </t>
                                    <t t-else="">
                                        <i class="fa fa-store me-1 text-muted" aria-hidden="true"/>
                                    </t>
                                    <t t-esc="cart_seller.company_name"/>
                                </span>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
    
    <!-- Cart seller group header component -->
    <template id="cart_seller_group_header" name="Cart Seller Group Header">
        <div class="smart-cart-seller-group mb-3 mt-4 first:mt-0">
            <div class="d-flex align-items-center justify-content-between p-3 rounded-top" 
                 style="background: linear-gradient(135deg, #714B67 0%, #9B6A8F 100%);">
                <div class="d-flex align-items-center text-white">
                    <t t-if="seller.store_logo">
                        <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % seller.id" 
                             t-att-alt="seller.company_name"
                             class="rounded-circle me-3 border border-2 border-white" 
                             style="width: 40px; height: 40px; object-fit: cover;"/>
                    </t>
                    <t t-else="">
                        <div class="rounded-circle me-3 d-flex align-items-center justify-content-center bg-white"
                             style="width: 40px; height: 40px;">
                            <i class="fa fa-store" style="color: #714B67;"/>
                        </div>
                    </t>
                    <div>
                        <h6 class="mb-0 fw-bold"><t t-esc="seller.company_name"/></h6>
                        <small class="opacity-75">
                            <t t-esc="len(seller_lines)"/> item<t t-if="len(seller_lines) != 1">s</t>
                        </small>
                    </div>
                </div>
                <a t-att-href="'/store/%s' % slug(seller)" class="btn btn-sm btn-light" target="_blank">
                    <i class="fa fa-external-link me-1"/>View Store
                </a>
            </div>
        </div>
    </template>
    
    <!-- ============================================================ -->
    <!-- CART SUMMARY - SHOW BREAKDOWN BY SELLER -->
    <!-- ============================================================ -->
    
    <template id="cart_summary_seller_breakdown" inherit_id="website_sale.total" name="Cart Summary Seller Breakdown">
        <xpath expr="//tr[@id='order_total']" position="before">
            <!-- Seller breakdown section -->
            <t t-set="cart_sellers" t-value="website_sale_order.order_line.mapped('product_id.product_tmpl_id.seller_id').filtered(lambda s: s.id) if website_sale_order else []"/>
            <tr t-if="cart_sellers and len(cart_sellers) > 1" class="smart-seller-breakdown-row">
                <td colspan="3" class="border-0 ps-0 pt-0 pb-3">
                    <div class="small text-muted mb-2">
                        <i class="fa fa-store me-1" aria-hidden="true"/>Breakdown by Store:
                    </div>
                    <t t-foreach="cart_sellers" t-as="breakdown_seller">
                        <t t-set="seller_lines" t-value="website_sale_order.order_line.filtered(lambda l: l.product_id.product_tmpl_id.seller_id.id == breakdown_seller.id)"/>
                        <t t-set="seller_subtotal" t-value="sum(seller_lines.mapped('price_subtotal'))"/>
                        <div class="d-flex justify-content-between align-items-center py-1 px-2 rounded" style="background: rgba(113, 75, 103, 0.05);">
                            <span class="small">
                                <t t-if="breakdown_seller.store_logo">
                                    <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % breakdown_seller.id" 
                                         class="rounded-circle me-1" style="width: 16px; height: 16px; object-fit: cover;"
                                         t-att-alt="breakdown_seller.company_name"/>
                                </t>
                                <t t-else="">
                                    <i class="fa fa-store me-1 text-muted" aria-hidden="true"/>
                                </t>
                                <t t-esc="breakdown_seller.company_name"/>
                                <span class="text-muted">(<t t-esc="len(seller_lines)"/>)</span>
                            </span>
                            <span class="small fw-bold" style="color: #714B67;">
                                <t t-esc="seller_subtotal" t-options="{'widget': 'monetary', 'display_currency': website_sale_order.currency_id}"/>
                            </span>
                        </div>
                    </t>
                </td>
            </tr>
        </xpath>
    </template>
    
    <!-- ============================================================ -->
    <!-- CHECKOUT - MULTI-SELLER ORDER INFO -->
    <!-- ============================================================ -->
    
    <template id="checkout_multi_seller_info" inherit_id="website_sale.checkout" name="Checkout Multi-Seller Info">
        <xpath expr="//div[@id='shop_checkout']" position="after">
            <t t-set="checkout_sellers" t-value="website_sale_order.order_line.mapped('product_id.product_tmpl_id.seller_id').filtered(lambda s: s.id)"/>
            
            <!-- Multi-seller order notice -->
            <div t-if="len(checkout_sellers) > 1" class="smart-multi-seller-checkout mt-4 mb-4">
                <div class="card border-0 shadow-sm" style="border-radius: 12px; overflow: hidden;">
                    <div class="card-header text-white py-3" style="background: linear-gradient(135deg, #17a2b8 0%, #138496 100%);">
                        <h5 class="mb-0">
                            <i class="fa fa-info-circle me-2"/>Order Summary by Store
                        </h5>
                    </div>
                    <div class="card-body">
                        <p class="text-muted mb-3">
                            Your order will be split into <strong><t t-esc="len(checkout_sellers)"/> separate orders</strong>, 
                            one for each store. This ensures faster processing and delivery from each seller.
                        </p>
                        
                        <!-- Seller order previews -->
                        <div class="row g-3">
                            <t t-foreach="checkout_sellers" t-as="checkout_seller">
                                <t t-set="seller_order_lines" t-value="website_sale_order.order_line.filtered(lambda l: l.product_id.product_tmpl_id.seller_id.id == checkout_seller.id)"/>
                                <t t-set="seller_order_subtotal" t-value="sum(seller_order_lines.mapped('price_subtotal'))"/>
                                
                                <div class="col-md-6">
                                    <div class="card h-100 border" style="border-radius: 8px;">
                                        <div class="card-body p-3">
                                            <div class="d-flex align-items-center mb-2">
                                                <t t-if="checkout_seller.store_logo">
                                                    <img t-att-src="'/web/image/marketplace.seller/%s/store_logo' % checkout_seller.id" 
                                                         t-att-alt="checkout_seller.company_name"
                                                         class="rounded-circle me-2" 
                                                         style="width: 32px; height: 32px; object-fit: cover; border: 1px solid #eee;"/>
                                                </t>
                                                <t t-else="">
                                                    <div class="rounded-circle me-2 d-flex align-items-center justify-content-center bg-light"
                                                         style="width: 32px; height: 32px;">
                                                        <i class="fa fa-store text-muted small"/>
                                                    </div>
                                                </t>
                                                <div>
                                                    <strong class="d-block small"><t t-esc="checkout_seller.company_name"/></strong>
                                                    <span class="text-muted small">
                                                        <t t-esc="len(seller_order_lines)"/> item<t t-if="len(seller_order_lines) != 1">s</t>
                                                    </span>
                                                </div>
                                            </div>
                                            
                                            <!-- Products list -->
                                            <div class="border-top pt-2 mt-2">
                                                <t t-foreach="seller_order_lines[:3]" t-as="preview_line">
                                                    <div class="d-flex align-items-center small py-1">
                                                        <img t-att-src="'/web/image/product.product/%s/image_64' % preview_line.product_id.id" 
                                                             class="rounded me-2" style="width: 24px; height: 24px; object-fit: cover;"/>
                                                        <span class="text-truncate flex-grow-1" style="max-width: 150px;">
                                                            <t t-esc="preview_line.name_short"/>
                                                        </span>
                                                        <span class="text-muted ms-1">×<t t-esc="int(preview_line.product_uom_qty)"/></span>
                                                    </div>
                                                </t>
                                                <div t-if="len(seller_order_lines) > 3" class="small text-muted mt-1">
                                                    + <t t-esc="len(seller_order_lines) - 3"/> more item<t t-if="len(seller_order_lines) - 3 != 1">s</t>
                                                </div>
                                            </div>
                                            
                                            <!-- Subtotal -->
                                            <div class="border-top pt-2 mt-2 d-flex justify-content-between">
                                                <span class="small text-muted">Subtotal:</span>
                                                <span class="fw-bold text-primary">
                                                    <t t-esc="seller_order_subtotal" t-options="{'widget': 'monetary', 'display_currency': website_sale_order.currency_id}"/>
                                                </span>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>
                        
                        <div class="alert alert-light mt-3 mb-0 small" role="alert">
                            <i class="fa fa-truck me-2 text-muted"/>
                            Each store will handle shipping separately. Delivery times may vary.
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
</odoo>
